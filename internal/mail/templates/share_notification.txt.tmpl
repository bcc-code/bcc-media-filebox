{{- /* Plain-text twin of the HTML mail. Same order, same wording, so a reader
       who sees only this one loses nothing but the styling. */ -}}
FileBox
{{if .SenderName}}{{.SenderName}} sent you a package{{else}}You've received files{{end}}
{{- if .PackageName}}
{{.PackageName}}
{{- end}}
{{if .Message}}
{{.Message}}
{{end}}
{{- if .Files}}
{{range .Files}}  {{.Name}}  ({{.SizeLabel}})
{{end}}  {{.FileLabel}}{{with .TotalLabel}} · {{.}}{{end}}
{{end}}
Download {{.FileLabel}}{{with .TotalLabel}} · {{.}}{{end}}:
{{.ShareURL}}

Expires {{.ExpiresOn}}{{if .MaxDownloads}} · {{.MaxDownloads}} downloads allowed{{end}}.
After that the files are no longer available and you will need to ask for a new
link.

--
Sent by FileBox{{if .SenderEmail}} on behalf of {{.SenderEmail}}{{end}}.
Reply to this email to reach {{if .SenderName}}{{.SenderName}}{{else}}the sender{{end}} directly.
